package com.synesis.hrmis.repository;

import com.synesis.hrmis.domain.SalaryUnit;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SalaryUnitRepository extends JpaRepository<SalaryUnit, Long> {
}
